using UnityEngine;
using System.Collections;
using UnityEditor;

/*
[CustomEditor(typeof(PatrolPath))]
public class PatrolPathEditor : Editor
{
    void OnSceneGUI()
    {
        PatrolPath path = (PatrolPath)target;
        Handles.color = Color.blue;

        for (int i = 0; i < path.PatrolPoints.Count; i++)
        {
            Handles.DrawLine(path.PatrolPoints[i].PatrolPointPosition, path.PatrolPoints[(i+1) % path.PatrolPoints.Count].PatrolPointPosition);
        }
    }
}
*/
